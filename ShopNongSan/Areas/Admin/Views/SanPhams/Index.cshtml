@model IEnumerable<SanPham>
@{
    ViewData["Title"] = "Sản phẩm";
    int page = ViewBag.Page ?? 1;
    int totalPages = ViewBag.TotalPages ?? 1;
    int totalCount = ViewBag.TotalCount ?? 0;
    string q = ViewBag.Q as string ?? "";
    int? dm = ViewBag.FilterDanhMucId as int?;
    int? th = ViewBag.FilterThuongHieuId as int?;

    string ImgUrl(string? p) =>
        string.IsNullOrWhiteSpace(p) ? Url.Content("~/images/no-image.png")
        : (p.StartsWith("/") ? Url.Content("~" + p) : Url.Content("~/images/" + p));
}

<div class="card shadow-sm mb-3">
    <div class="card-body">
        <form method="get" class="row g-3 align-items-end">
            <!-- Tìm kiếm -->
            <div class="col-lg-5">
                <label class="form-label mb-1">Tìm kiếm</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-search"></i></span>
                    <input type="text" name="q" value="@q" class="form-control"
                           placeholder="Tên sản phẩm, danh mục, thương hiệu..." />
                </div>
            </div>

            <!-- Danh mục -->
            <div class="col-lg-3">
                <label class="form-label mb-1">Danh mục</label>
                <select name="danhMucId" asp-items="ViewBag.DanhMucId" class="form-select select2">
                    <option value="">-- Tất cả --</option>
                </select>
            </div>

            <!-- Thương hiệu -->
            <div class="col-lg-3">
                <label class="form-label mb-1">Thương hiệu</label>
                <select name="thuongHieuId" asp-items="ViewBag.ThuongHieuId" class="form-select select2">
                    <option value="">-- Tất cả --</option>
                </select>
            </div>

            <!-- Lọc + Xóa bộ lọc -->
            <div class="col-md-4 col-lg-3 col-xl-2">
                <div class="d-flex gap-2">
                    <input type="hidden" name="page" value="1" />
                    <button type="submit" class="btn btn-primary px-3 btn-nowrap">
                        <i class="bi bi-funnel"></i> Lọc
                    </button>
                    <a asp-area="Admin" asp-controller="SanPhams" asp-action="Index"
                       class="btn btn-danger px-3 btn-nowrap">
                        <i class="bi bi-x-circle"></i> Xóa bộ lọc
                    </a>
                </div>
            </div>

            <!-- Tổng số + Thêm mới -->
            <div class="col-12 d-flex justify-content-between align-items-center">
                <span class="small text-muted">Tổng: @totalCount</span>
                <a asp-area="Admin" asp-controller="SanPhams" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Thêm mới
                </a>
            </div>
        </form>
    </div>
</div>

<!-- BẢNG -->
<div class="table-responsive shadow-sm rounded">
    <table class="table table-hover table-bordered align-middle mb-0">
        <thead class="table-success">
            <tr class="text-center">
                <th style="min-width:260px">Tên</th>
                <th>Danh mục</th>
                <th>Thương hiệu</th>
                <th class="text-center">Giá</th>
                <th class="text-center">Tồn</th>
                <th class="text-center">Ảnh</th>
                <th class="text-center" style="width:160px">Thao tác</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var sp in Model)
            {
                var stockCls = sp.SoLuongTon == 0 ? "bg-danger"
                : sp.SoLuongTon <= 20 ? "bg-warning text-dark"
                : "bg-success";

                <tr>
                    <td class="fw-semibold">
                        @sp.Ten
                        <div class="text-muted small">#@sp.Id</div>
                    </td>
                    <td class="text-muted">@sp.DanhMuc?.Ten</td>
                    <td class="text-muted">@sp.ThuongHieu?.Ten</td>
                    <td class="text-end fw-semibold">@sp.Gia.ToString("N0") đ</td>
                    <td class="text-center"><span class="badge @stockCls">@sp.SoLuongTon</span></td>
                    <td class="text-center">
                        <img src="@ImgUrl(sp.HinhAnh)" alt="@sp.Ten"
                             class="rounded" style="width:60px;height:60px;object-fit:cover" />
                    </td>
                    <td class="text-end">
                        <div class="btn-group">
                            <a asp-action="Edit" asp-route-id="@sp.Id" class="btn btn-sm btn-warning text-dark">
                                <i class="bi bi-pencil-square"></i> Sửa
                            </a>
                            <a asp-action="Delete" asp-route-id="@sp.Id" class="btn btn-sm btn-danger">
                                <i class="bi bi-trash"></i> Xóa
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- PHÂN TRANG + ĐI TỚI TRANG -->
@if (totalPages > 1)
{
    <div class="mt-3 d-flex justify-content-between align-items-center flex-wrap gap-2">
        <nav>
            <ul class="pagination mb-0">
                <li class="page-item @(page <= 1 ? "disabled" : "")">
                    <a class="page-link"
                       asp-action="Index"
                       asp-route-page="@(page - 1)"
                       asp-route-q="@q"
                       asp-route-danhMucId="@dm"
                       asp-route-thuongHieuId="@th">«</a>
                </li>
                @for (int i = Math.Max(1, page - 2); i <= Math.Min(totalPages, page + 2); i++)
                {
                    <li class="page-item @(i == page ? "active" : "")">
                        <a class="page-link"
                           asp-action="Index"
                           asp-route-page="@i"
                           asp-route-q="@q"
                           asp-route-danhMucId="@dm"
                           asp-route-thuongHieuId="@th">@i</a>
                    </li>
                }
                <li class="page-item @(page >= totalPages ? "disabled" : "")">
                    <a class="page-link"
                       asp-action="Index"
                       asp-route-page="@(page + 1)"
                       asp-route-q="@q"
                       asp-route-danhMucId="@dm"
                       asp-route-thuongHieuId="@th">»</a>
                </li>
            </ul>
        </nav>

        <!-- Form đi tới trang (bên phải) -->
        <form method="get" class="d-flex align-items-center gap-2">
            <input type="hidden" name="q" value="@q" />
            <input type="hidden" name="danhMucId" value="@dm" />
            <input type="hidden" name="thuongHieuId" value="@th" />
            <div class="input-group" style="width: 180px;">
                <input type="number" name="page" min="1" max="@totalPages" value="@@page" class="form-control" />
                <button type="submit" class="btn btn-primary btn-nowrap">
                    <i class="bi bi-box-arrow-in-right"></i> Đi tới
                </button>
            </div>
            <span class="text-muted small">/ @totalPages</span>
        </form>
    </div>
}

@section Scripts {
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />

    <!-- jQuery (cần cho Select2; bỏ qua nếu dự án đã có) -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.7.1/dist/jquery.min.js"></script>
    <!-- Select2 -->
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

    <script>
        $(function () {
            // Khởi tạo Select2 cho dropdown filter
            $('.select2').select2({
                placeholder: '-- Tất cả --',
                allowClear: true,
                width: '100%'
            });
        });
    </script>
}

<style>
    .btn-nowrap { white-space: nowrap; }

    /* Ép màu nút nền đầy */
    .btn-primary {
        background-color: #0d6efd !important;
        border-color: #0d6efd !important;
        color: #fff !important;
    }
    .btn-danger {
        background-color: #dc3545 !important;
        border-color: #dc3545 !important;
        color: #fff !important;
    }
    .btn-warning {
        background-color: #ffc107 !important;
        border-color: #ffc107 !important;
        color: #212529 !important;
    }

    /* Header bảng xanh lá */
    .table thead.table-success th {
        background-color: #198754 !important;
        color: #fff !important;
    }

    /* Phân trang xanh dương */
    .pagination .page-item.active .page-link {
        background-color: #0d6efd !important;
        border-color: #0d6efd !important;
        color: #fff !important;
    }
    .pagination .page-link { color: #0d6efd; }
    .pagination .page-link:hover {
        background-color: #0d6efd !important;
        color: #fff !important;
    }

    /* Tối ưu Select2 cho form-control cảm giác đồng bộ */
    /* Ép chiều cao cho select2 khớp input bootstrap */
    .select2-container .select2-selection--single {
        height: 38px !important;
    }

    .select2-container--default .select2-selection--single .select2-selection__rendered {
        line-height: 38px !important;
    }

    .select2-container--default .select2-selection--single .select2-selection__arrow {
        height: 38px !important;
    }

</style>
